- uploading the same file (to show that it won't upload if it's there) works after a refresh in chromium base browsers
- payload too large error with uploads doesn't show error page in firefox, showcase it in chromium based browser
- check the "//inspect this" and find the issue with the cgis not cleaning up properly after server restarts